{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Debug hello (LLDB)",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/build/dev/hello",
        "args": [],
        "cwd": "${workspaceFolder}",
        "preLaunchTask": "build:dev"
      }
    ]
  }